<h1>Customers#edit</h1>
<p>Find me in app/views/customers/edit.html.erb</p>
<% if @customer.errors.any? %>
  <%= @customer.errors.count %>errors prohibited this customer from being saved:
  <ul>
   <% @customer.errors.full_messages.each do |message| %>
      <li><%= message %></li>
   <% end %>
  </ul>
<% end %>

<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">

      <h2>Customer info</h2>
     <%= form_with model: @customer do |f| %>

      <div class="form-group">
      <h4>Name</h4>
      <%= f.text_field :name, class: 'form-control customer_name', autofocus: true, id:"inputName" %>
      </div>

      
      <%= f.submit "登録" %>
      <%= f.submit "退会" %>
     <% end %>
    </div>
  </div>
</div>